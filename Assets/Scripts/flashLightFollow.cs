using UnityEngine;
using System.Collections;

public class flashLightFollow : MonoBehaviour {
	public GameObject player;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
//		transform.position = player.transform.position;
//		//transform.rotation = player.transform.rotation;
//		var zDistance = 10.0;
//		Vector3 mousePos = Input.mousePosition; 
//
//		Vector3 mousePosNew = Input.mousePosition;
//		mousePosNew.Set (mousePosNew.x, mousePosNew.y, 10);
//		//transform.LookAt(Camera.main.ScreenToWorldPoint(mousePos.x, mousePos.y, zDistance));
//		transform.LookAt(Camera.main.ScreenToWorldPoint(mousePosNew));
//		Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition); 
//		RaycastHit hit;
	 // make the spotlight do whatever using hit.point } 
	}
}
